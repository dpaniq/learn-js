'use strict'

/*
================================ Data Types: Array | Тип данных: массив

Объявление массива:
    let myArray = [] или new Array(n)
    Пример:
    let cities = ['riga', 'jurmala', 'daugava']
    let cities = new Array('riga', 'jurmala', 'daugava')
    let cities = new Array(5) ---> ',,,,'

Методы массива:

.concat  - сложение массивов x.concat(y, 'конечное значение') // concatination
.push - добавляет в конец массива элемент
.pop - удаляет и возвращает последний элемент массива
.splice -
.join('--->') - соединить элементы через разделитель и вывести строку
